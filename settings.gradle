rootProject.name = 'kimzing-framework'
include 'kimzing-practice'
include 'kimzing-shop'
include 'kimzing-spring'

rootProject.children.each {project ->
    project.buildFileName = "${project.name}.gradle"
}

